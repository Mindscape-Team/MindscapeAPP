using UnityEngine;

public class UI_SplashAnimation : MonoBehaviour
{
    [SerializeField] private GameObject animatedObject;

    //[SerializeField] private GameObject onBoardingBackground;
    //[SerializeField] private GameObject onBoardingFirstScreenBody;

    void Start()
    {
        //logoTitle.transform.localPosition = new Vector2(0, -Screen.height);
        //logoTitle.LeanMoveLocalY(0, 0.5f).setEaseOutExpo().delay = 0.1f;

        //onBoardingBackground.transform.localPosition = new Vector2(-Screen.width, 0);
        //onBoardingBackground.LeanMoveLocalX(0, 0.5f).setEaseOutExpo().delay = 1.5f;

        //onBoardingFirstScreenBody.transform.localPosition = new Vector2(0, -Screen.height);
        //onBoardingFirstScreenBody.LeanMoveLocalY(0, 0.25f).setEaseOutExpo().delay = 1.75f;
    }
}
